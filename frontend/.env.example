# Backend URL — in dev this is empty (Vite proxy handles it)
# In production set this to your Render backend URL, e.g.:
# VITE_API_URL=https://swasthya-flow-backend.onrender.com
VITE_API_URL=

# Frontend URL (used for generating QR codes etc.)
VITE_FRONTEND_URL=http://localhost:5173
